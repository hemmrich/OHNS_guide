import 'package:flutter/material.dart';

class Pediatrics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Pediatrics'), backgroundColor: Colors.lightBlue),
        body: Text('Pediatrics screen')
    );
  }
}